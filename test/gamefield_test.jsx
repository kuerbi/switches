import React from "react"
import { expect } from "chai"
import { mount } from "enzyme"
import { GameField } from "gamefield"


describe("Gamefield component", () => {
  it("shows 25 switches TODO", () => {
    const wrapper = mount(<GameField />);
  });
});
